<?xml version="1.0" encoding="UTF-8" ?>   
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="com.demo.my.base.mybatis.mapper.ds1mapper.BlogMenuMapper">

<resultMap type="com.demo.my.base.bean.BlogMenu" id="BlogMenuBaseMapper">
	<id property="id" column="ID" />
	<result property="name" column="NAME" />
</resultMap>  

<insert id="insert">
	insert into blog_menu(NAME) values(#{name})
</insert>

<update id="update">
	update blog_menu
	<set>
		<if test="name != null and name != ''">  
	        NAME=#{name},
	    </if>
	</set>
	 where ID=#{id}
</update>

<select id="getBeanListByParm" resultMap="BlogMenuBaseMapper">
	SELECT * FROM blog_menu ORDER BY ID ASC
</select>

<select id="getById">
	SELECT * FROM blog_menu where ID=#{id}
</select>

</mapper>